/* import React from 'react';
import { storiesOf } from '@kadira/storybook';

import Wrapper from './';

storiesOf('Wrapper', module)
  .addWithInfo('with children', () => (
    <Wrapper>Inside Wrapper</Wrapper>
  ))

  .addWithInfo('xlarge size', () => (
    <Wrapper size="xlarge">Inside Wrapper</Wrapper>
  ))

  .addWithInfo('small size', () => (
    <Wrapper size="small">Inside Wrapper</Wrapper>
  ))
; */
