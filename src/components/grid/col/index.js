/* import React, { PropTypes } from 'react';
import classNames from 'classnames';

import styles from './styles.scss';

const Col = ({ children, className, column }) => {
  const deconstruct = (columnArray) => {
    // Open 0th array item
    // Open 0th item add style: {}
  };

  return (
    <div className={className}>{children}</div>
  );
};

// [$above-breakpoint (px or string), $columns, $offset ]


Col.PropTypes = {
  children: PropTypes.node,
  className: PropTypes.string,
  column: PropTypes.arrayOf(PropTypes.array),
};

// Col.defaultProps = {

// }

export default Col; */
